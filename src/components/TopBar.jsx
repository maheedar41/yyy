import React from 'react'
import { Link } from 'react-router-dom'

// Bottom bar removed — component kept as a placeholder in case you want to add items here later.
export default function BottomBar(){
  return null
}
